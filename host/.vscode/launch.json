{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "sftool",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/build/sftool/sftool",
            "args": [
                "-s", "64"
            ],
            "cwd": "${workspaceFolder}"           
        }
    ]
}